import { type Component } from 'solid-js';

export interface AppContainerProps {


}

export const AppContainer: Component<AppContainerProps> = (props) => {
  return <div class="app-container">{ }</div>
};

export default AppContainer;
